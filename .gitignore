# Devvit + Node
node_modules/
.devvit/
dist/
build/
*.log
.devvit-cache/

# Coverage / test artifacts
coverage/
.nyc_output/

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
*~

# Local env (must never commit OpenAI keys)
.env
.env.local
.env.*.local

# Devvit auth + settings (Devvit stores in ~/.devvit/, but defensive ignore)
.devvit-auth
*.devvit-token

# Generated docs
docs/*.html

# Chrome verify/recording automation (Python venv + Playwright artifacts).
# Kept on disk locally for demo capture but never published — these read the
# operator's live Reddit session cookies at runtime and the automation approach
# itself stays private.
.venv-chrome-auth/
playwright/
scripts/__pycache__/
scripts/chrome-reddit-*.py
